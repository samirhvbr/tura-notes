[package]
name = "notes-app"
version = "0.0.0"
description = "notes — a local-first Markdown editor"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
name = "notes_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
notes-model = { workspace = true }
notes-core = { workspace = true }
notes-sync-client = { path = "../../../crates/notes-sync-client" }
notes-markdown = { workspace = true }
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-shell = "2"
serde = { workspace = true }
serde_json = { workspace = true }
pdf-extract = { workspace = true }

[dev-dependencies]
# The PDF refusal tests write a throwaway file; `tempfile` is already the
# workspace's choice for exactly that.
tempfile = { workspace = true }

[target.'cfg(any(target_os = "macos", target_os = "linux", windows))'.dependencies]
tauri-plugin-updater = "2.10.1"
