// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BaseRev } from "./BaseRev";
import type { IoKind } from "./IoKind";
import type { LockWait } from "./LockWait";
import type { NoteId } from "./NoteId";
import type { ReadOnlyReason } from "./ReadOnlyReason";
import type { UnavailableReason } from "./UnavailableReason";

/**
 * Every command returns `Result<T, CoreError>`.
 *
 * `code` is the contract; the frontend maps it to an i18n key and never
 * inspects a message (`ARCHITECTURE.md` §7.3). `Internal` is a bug rather than
 * a state, and a test that expects one fails CI.
 */
export type CoreError = { "code": "invalid_path", path: string, reason: string, } | { "code": "outside_root", path: string, } | { "code": "symlink_not_followed", path: string, } | { "code": "not_found", path: string, } | { "code": "already_exists", path: string, } | { "code": "dirty_buffers", note_ids: Array<NoteId>, count: number, } | { "code": "conflict", note_id: NoteId, disk_rev: BaseRev, } | { "code": "read_only", note_id: NoteId, reason: ReadOnlyReason, } | { "code": "unavailable", root: string, reason: UnavailableReason, } | { "code": "no_workspace" } | { "code": "lock_timeout", what: LockWait, } | { "code": "not_settled", passes: number, queued: number, } | { "code": "unsupported", cap: string, } | { "code": "io", op: string, path: string, kind: IoKind, } | { "code": "schema_ahead", store: string, found: number, supported: number, } | { "code": "internal", message: string, };
